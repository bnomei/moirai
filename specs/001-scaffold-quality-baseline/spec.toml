spec_id = "001-scaffold-quality-baseline"
name = "Moirai single-crate scaffold and quality baseline"
depends_on = []
global_scope = ["Cargo.toml", "src", "tests", "benches", ".github", "docs", "README.md"]
spec_kind = "feature"
execution_policy = "auto-continue"
fanout_policy = "parallel-when-ready"
human_checkpoint = "none"
commit_policy = "after-validation"
review_policy = "required"

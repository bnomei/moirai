+++
id = "T004"
title = "Add CI, documentation, and benchmark-harness quality gates"
status = "todo"
scope = [".github", "benches", "docs", "Cargo.toml"]
depends = ["T001"]
covers = ["R002", "R006", "R007"]
verification_mode = "validator"
verification_status = "pending"
worker_reasoning_effort = "medium"
worker_model = "terra"
test_strategy = "existing-tests-only"
slice = "normal"
reuse_targets = []
read_allowlist = ["Cargo.toml", "README.md"]
bundle_with = []
commit = ""
commit_review = ""
+++

## Context

Create legible CI jobs for format, strict Clippy over all targets/features, core/std/testkit/all-
feature tests, docs with warnings denied, and a Rust 1.75 library-only no-default check. Current
stable owns quality tooling; the MSRV job does not. Add only a benchmark harness/documentation
scaffold—never placeholder benchmark claims or coverage percentages.

## DoD

- CI expresses the required toolchain/feature split.
- Documentation explains the future coverage/performance proof boundary honestly.
- Benchmark wiring does not impose a runtime dependency or public API.

## Validation

```sh
cargo fmt --all -- --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test --no-default-features
cargo test --features std
cargo test --features testkit
cargo test --all-features
RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --all-features
cargo bench --no-run
cargo +1.75 check --lib --no-default-features
```

## Escalate If

The preferred CI action or dev dependency cannot support the declared MSRV/toolchain separation.

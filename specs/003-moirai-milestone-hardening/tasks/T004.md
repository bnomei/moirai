+++
id = "T004"
title = "Reconcile integration truth and clean no-default warnings"
status = "done"
scope = ["docs/ARCHITECTURE.md", "specs/002-moirai-integration-readiness/tasks/T008.md", "src/world/flush.rs", "tests/public_api.rs", "tests/readme.rs"]
depends = ["T003"]
covers = ["R005", "R006", "R007", "R008"]
verification_mode = "required"
verification_status = "passed"
worker_reasoning_effort = "high"
worker_model = "sol"
test_strategy = "red-green-refactor"
slice = "follow-up"
reuse_targets = ["docs/ARCHITECTURE.md", "tests/public_api.rs"]
read_allowlist = ["Cargo.toml", "src/app.rs", "src/entity", "src/schedule", "src/world/flush.rs", "src/world/owner.rs", "docs/ARCHITECTURE.md", "specs/002-moirai-integration-readiness/tasks/T008.md", "tests/public_api.rs", "tests/readme.rs"]
bundle_with = []
commit = ""
commit_review = ""
implemented_by = "worker:/root/reconcile_integration_truth"
verified_by = "/root/review_integration_truth"
completed_at = "2026-07-13T20:07:58+01:00"
last_lease_id = "l_02337e5b3a32"
+++

## Context

Correct the milestone review's stale integration claims: nonexistent `AppBuilder::register_component`
and `System::in_state` methods, the old eight-byte ownerless EntityId description, Rc-only ownership,
and spec 002 T008's statement that EntityId has no owner token. Describe the actual owner-bearing
16-byte opaque handle and checked current authoring paths without promising downstream cutover or
performance. Add executable API-shape assertions where they materially prevent recurrence.

Remove the known no-default test warning caused by a feature-sensitive `ChangeTick` import in
`src/world/flush.rs`.

## DoD

- Architecture/spec wording and public API tests agree with current source.
- No stale ownerless/8-byte/global-atomic-free claim remains in the scoped documents.
- `cargo test --no-default-features` emits no known unused-import warning.
- No performance or completed-downstream-integration claim is added.

## Validation

Run public/readme tests, no-default tests, all-feature tests, strict Clippy, rustdoc, and Rust 1.75
no-default check. A fresh Sol/high validator must compare every corrected claim to source.

## Escalate If

Truthful correction requires changing a frozen public contract rather than documentation or tests.

+++
id = "T007"
title = "Add entity and dynamic-ID queries"
status = "todo"
scope = ["src/query", "src/world/query", "src/world/access.rs", "src/world/mod.rs", "src/lib.rs", "src/prelude.rs", "tests/query.rs", "tests/query_model.rs", "tests/query_cache.rs", "tests/query_result_cache.rs", "tests/parity_gaps.rs", "tests/public_api.rs", "tests/prelude.rs"]
depends = ["T006"]
covers = ["R007", "R010", "R011", "R012"]
verification_mode = "required"
verification_status = "pending"
worker_reasoning_effort = "high"
worker_model = "sol"
test_strategy = "red-green-refactor"
slice = "normal"
reuse_targets = ["src/query/spec.rs", "src/query/cursor.rs", "src/world/query/ids.rs", "src/world/query/spec.rs", "src/world/query/plan.rs"]
read_allowlist = ["Cargo.toml", "src/component", "src/entity/id.rs", "src/query", "src/world/access.rs", "src/world/mod.rs", "src/world/query", "src/lib.rs", "src/prelude.rs", "tests/query.rs", "tests/query_model.rs", "tests/query_cache.rs", "tests/query_result_cache.rs", "tests/parity_gaps.rs", "tests/public_api.rs", "tests/prelude.rs"]
bundle_with = []
commit = ""
commit_review = ""
+++

## Context

Add `World::query_ids(spec, params) -> QueryIds` yielding `EntityId` and
`World::query_entities(spec, params) -> QueryEntities` yielding immutable `EntityRef`. An empty spec
enumerates every live entity; tag-only and exact-ID specs require no typed primary component.
`EntityRef` exposes `id`, checked `has<T>`, and checked immutable `get<T>` only. It must not permit
mutation or outlive its World borrow.

Extend `QuerySpec` with checked `ComponentId` selectors `with_id`, `without_id`, `with_tag_id`,
`without_tag_id`, `added_id`, and `changed_id`; retain the typed builders. Store multiple added or
changed selectors and match each group with OR semantics. Combining any added group with any changed
group remains `QueryError::ConflictingFilters`. Validate ComponentId owner, generation, registration,
tag kind, overlaps, exact-ID policy, and stale/foreign entity handling before iteration.

Entity queries participate in `QueryParams`, partial-iteration cursor commit rules, exact-ID policy,
`QueryCache`, and `QueryResultCache`. Add entity-spec cache builders and
`QueryCursor::{for_entities_from_start, for_entities_from_now, fork}`. Make raw fingerprint-based
cursor constructors crate-private. Do not add optional projection wrappers, Query3+, arbitrary
mutable tuples, name-based identity, raw component indices, or a Bevy query DSL.

## DoD

- Empty, tag-only, dynamic-ID, exact-ID, cached, cursor, and partial entity queries work across table
  and sparse entities in deterministic order.
- EntityRef enables optional immutable reads without selecting a primary component.
- Added/changed OR groups use the captured query window and preserve cursor exhaustion semantics.
- Wrong-owner/stale ComponentId, EntityId, cursor, and caches fail contextually.
- Existing Query1/Query2 and mutable closure APIs remain source-compatible except for T001's removal
  of unnecessary Clone bounds.

## Validation

Add entity-only, tag-only, incomplete-entity, optional-read, dynamic-ID, OR-filter, exact-ID,
cache/cursor fork, partial iteration, and cross-owner tests, then run:

```sh
cargo test --test query --test query_model --test query_cache --test query_result_cache --test parity_gaps --test public_api --test prelude
cargo test --all-features
cargo clippy --all-targets --all-features -- -D warnings
cargo +1.75 check --lib --no-default-features
```

Orchid must run a fresh Sol/high validator, distinct from the worker, against the canonical worker
report. The validator must inspect the diff and `git-touched`, reproduce focused tests, and review
borrow lifetimes, plan fingerprints, OR windows, cursor commit/fork, cache invalidation, exact order,
owner checks, and API restraint. On `needs_fix`, create a repair packet and rerun focused validation
plus a new Sol/high review; use Terra/medium only for a bounded local defect with no contract choice.
Stop after three repair rounds, the same root defect twice, a blocked verdict, attribution conflict,
or a required architecture decision outside this spec. Do not stage or commit before `pass`.

## Escalate If

Safe EntityRef iteration requires unsafe aliasing, a no-primary plan cannot share existing cache and
cursor invariants, consumer needs prove Query3+/mutable dynamic projection is unavoidable, or
ComponentId validation would require exposing registry internals.

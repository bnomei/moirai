# Intake

Research the Phase 3/4 boundary: complete World data lifecycle, then execute it through a separately
owned compiled Schedule inside App. The handoff must make mutation timing, flush transactionality,
event observation, fixed ticks, fallible execution, and no-unsafe ownership explicit.

Out of scope: complete query internals, host-specific stages/state, platform profiler FFI, and
downstream adapters.


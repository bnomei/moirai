# Intake

Research a safe public query facade that preserves the useful pd-asteroids Query1/Query2/spec and
dual-cache behavior across sparse/table/tag/mixed storage without exposing plans, raw cache keys,
or unsafe mutable iterator machinery.

The same handoff owns hot/cold/cache/allocation benchmark proof for queries. Full-crate coverage and
parity closure remain Phase 6.

